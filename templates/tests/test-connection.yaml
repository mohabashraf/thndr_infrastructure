apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thndr-app.fullname" . }}-test-connection"
  labels:
    {{- include "thndr-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thndr-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
